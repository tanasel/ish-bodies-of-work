/* ============================================================
   Shared-shelf configuration.
   Paste your Apps Script Web App URL between the quotes to turn
   on the shared, whole-team shelf. Leave it empty to run in
   "this browser only" mode (additions stay local).
   See SETUP-SHARING.md for the 4-step setup.
   ============================================================ */
window.BOW_CONFIG = {
  endpoint: "" // e.g. "https://script.google.com/macros/s/AKfy.../exec"
};
